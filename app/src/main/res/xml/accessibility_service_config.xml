<?xml version="1.0" encoding="utf-8"?>
<!--
    Deliberately narrow (Play Accessibility API policy): the service only reacts
    to the accessibility button. No accessibilityEventTypes (no events are
    delivered) and no window content access. canTakeScreenshot is the only
    capability beyond the button itself.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRequestAccessibilityButton"
    android:canRetrieveWindowContent="false"
    android:canTakeScreenshot="true"
    android:description="@string/accessibility_service_description"
    android:settingsActivity="io.github.v_inn.rubify.MainActivity"
    android:summary="@string/accessibility_service_summary" />
